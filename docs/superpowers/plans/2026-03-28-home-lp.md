# ホームLP 実装計画

> **エージェント向け:** 必須サブスキル: superpowers:subagent-driven-development（推奨）または superpowers:executing-plans を使用してタスクごとに実装。ステップは `- [ ]` チェックボックス構文で追跡。

**目標:** ホームページを9つのコンテンツセクション＋固定ナビゲーション＋フッターを備えたプレミアムなシングルページLPに再構築する。

**アーキテクチャ:** 新規コンポーネントを `src/components/home/` に、新規フックを `src/hooks/` に、新規APIルートを `src/app/api/contact/` に作成。既存コンポーネントは変更なし。`src/app/page.tsx` を新しい Home* コンポーネント群で置き換え、`LanguageProvider` でラップ。

**技術スタック:** Next.js 16, React 19, TypeScript (strict), Tailwind CSS v4, Framer Motion 12, Vitest + React Testing Library, Resend（メール送信）

**仕様書:** `docs/superpowers/specs/2026-03-28-home-lp-design.md`

---

## ファイル構成

```
src/
  hooks/
    useLanguage.ts              - JP/EN切り替え用React context
    useLanguage.test.ts
    useActiveSection.ts         - ナビハイライト用IntersectionObserver
    useActiveSection.test.ts
    useMagneticButton.ts        - マグネティックホバー効果
    useMagneticButton.test.ts
  components/home/
    HomeNavigation.tsx           - 固定ヘッダー
    HomeNavigation.test.tsx
    HomeHero.tsx                 - ヒーローセクション
    HomeHero.test.tsx
    HomeConcept.tsx              - コンセプトセクション
    HomeConcept.test.tsx
    HomeKeyNumbers.tsx           - キーナンバーセクション
    HomeKeyNumbers.test.tsx
    HomeFacility.tsx             - コート・設備セクション
    HomeFacility.test.tsx
    HomeServices.tsx             - サービス一覧セクション（5つ）
    HomeServices.test.tsx
    HomePricing.tsx              - 料金セクション
    HomePricing.test.tsx
    HomeFounder.tsx              - ファウンダーセクション
    HomeFounder.test.tsx
    HomeAccess.tsx               - アクセスセクション
    HomeAccess.test.tsx
    HomeContact.tsx              - お問い合わせセクション
    HomeContact.test.tsx
    HomeFooter.tsx               - フッター
    HomeFooter.test.tsx
  app/
    page.tsx                     - 変更: 新規コンポーネント構成に置き換え
    globals.css                  - 変更: ヒーロー用カスタムカーソルスタイル追加
    api/contact/
      route.ts                   - お問い合わせフォームAPI
      route.test.ts
```

---

### タスク 1: useLanguage フック

**ファイル:**
- 新規: `src/hooks/useLanguage.ts`
- 新規: `src/hooks/useLanguage.test.ts`

- [ ] **ステップ 1: 失敗するテストを書く**

```typescript
// src/hooks/useLanguage.test.ts
import { describe, it, expect } from "vitest";
import { renderHook, act } from "@testing-library/react";
import { render, screen } from "@testing-library/react";
import React from "react";
import { LanguageProvider, useLanguage } from "./useLanguage";
import type { ReactNode } from "react";

function wrapper({ children }: { children: ReactNode }) {
  return <LanguageProvider>{children}</LanguageProvider>;
}

describe("useLanguage", () => {
  it("デフォルトは 'ja'", () => {
    const { result } = renderHook(() => useLanguage(), { wrapper });
    expect(result.current.language).toBe("ja");
  });

  it("'en' に切り替えて 'ja' に戻る", () => {
    const { result } = renderHook(() => useLanguage(), { wrapper });

    act(() => result.current.toggleLanguage());
    expect(result.current.language).toBe("en");

    act(() => result.current.toggleLanguage());
    expect(result.current.language).toBe("ja");
  });

  it("複数のコンシューマーに一貫した状態を提供する", () => {
    function DisplayLang() {
      const { language } = useLanguage();
      return <span data-testid="lang">{language}</span>;
    }
    function ToggleButton() {
      const { toggleLanguage } = useLanguage();
      return <button onClick={toggleLanguage}>toggle</button>;
    }

    render(
      <LanguageProvider>
        <DisplayLang />
        <ToggleButton />
      </LanguageProvider>
    );

    expect(screen.getByTestId("lang")).toHaveTextContent("ja");
  });

  it("LanguageProvider外で使用するとエラーを投げる", () => {
    const spy = vi.spyOn(console, "error").mockImplementation(() => {});
    expect(() => renderHook(() => useLanguage())).toThrow(
      "useLanguage must be used within a LanguageProvider"
    );
    spy.mockRestore();
  });
});
```

- [ ] **ステップ 2: テストが失敗することを確認**

実行: `npx vitest run src/hooks/useLanguage.test.ts`
期待: FAIL - モジュールが見つからない

- [ ] **ステップ 3: 最小限の実装を書く**

```typescript
// src/hooks/useLanguage.ts
"use client";

import { createContext, useContext, useState, useCallback } from "react";
import type { ReactNode } from "react";

type Language = "ja" | "en";

interface LanguageContextValue {
  language: Language;
  toggleLanguage: () => void;
}

const LanguageContext = createContext<LanguageContextValue | null>(null);

interface LanguageProviderProps {
  children: ReactNode;
}

export function LanguageProvider({ children }: LanguageProviderProps) {
  const [language, setLanguage] = useState<Language>("ja");

  const toggleLanguage = useCallback(() => {
    setLanguage((prev) => (prev === "ja" ? "en" : "ja"));
  }, []);

  return (
    <LanguageContext.Provider value={{ language, toggleLanguage }}>
      {children}
    </LanguageContext.Provider>
  );
}

export function useLanguage(): LanguageContextValue {
  const context = useContext(LanguageContext);
  if (!context) {
    throw new Error("useLanguage must be used within a LanguageProvider");
  }
  return context;
}
```

- [ ] **ステップ 4: テストがパスすることを確認**

実行: `npx vitest run src/hooks/useLanguage.test.ts`
期待: PASS（4テスト）

- [ ] **ステップ 5: コミット**

```bash
git add src/hooks/useLanguage.ts src/hooks/useLanguage.test.ts
git commit -m "feat: add useLanguage hook for JP/EN toggle"
```

---

### タスク 2: useActiveSection フック

**ファイル:**
- 新規: `src/hooks/useActiveSection.ts`
- 新規: `src/hooks/useActiveSection.test.ts`

- [ ] **ステップ 1: 失敗するテストを書く**

```typescript
// src/hooks/useActiveSection.test.ts
import { describe, it, expect, vi, beforeEach } from "vitest";
import { renderHook, act } from "@testing-library/react";
import { useActiveSection } from "./useActiveSection";

let observerCallback: IntersectionObserverCallback;
const mockObserve = vi.fn();
const mockDisconnect = vi.fn();

beforeEach(() => {
  vi.clearAllMocks();
  global.IntersectionObserver = vi.fn((callback) => {
    observerCallback = callback;
    return {
      observe: mockObserve,
      disconnect: mockDisconnect,
      unobserve: vi.fn(),
      root: null,
      rootMargin: "",
      thresholds: [],
      takeRecords: vi.fn(),
    };
  });
});

const SECTION_IDS = ["concept", "facility", "services", "pricing", "access", "contact"];

describe("useActiveSection", () => {
  it("初期値は空文字列", () => {
    const { result } = renderHook(() => useActiveSection(SECTION_IDS));
    expect(result.current).toBe("");
  });

  it("提供された全セクションIDを監視する", () => {
    SECTION_IDS.forEach((id) => {
      const el = document.createElement("section");
      el.id = id;
      document.body.appendChild(el);
    });

    renderHook(() => useActiveSection(SECTION_IDS));
    expect(mockObserve).toHaveBeenCalledTimes(SECTION_IDS.length);

    SECTION_IDS.forEach((id) => {
      const el = document.getElementById(id);
      if (el) document.body.removeChild(el);
    });
  });

  it("要素が交差した時にアクティブセクションを更新する", () => {
    const section = document.createElement("section");
    section.id = "concept";
    document.body.appendChild(section);

    const { result } = renderHook(() => useActiveSection(SECTION_IDS));

    act(() => {
      observerCallback(
        [{ isIntersecting: true, target: section }] as unknown as IntersectionObserverEntry[],
        {} as IntersectionObserver
      );
    });

    expect(result.current).toBe("concept");
    document.body.removeChild(section);
  });

  it("アンマウント時にdisconnectする", () => {
    const { unmount } = renderHook(() => useActiveSection(SECTION_IDS));
    unmount();
    expect(mockDisconnect).toHaveBeenCalled();
  });
});
```

- [ ] **ステップ 2: テストが失敗することを確認**

実行: `npx vitest run src/hooks/useActiveSection.test.ts`
期待: FAIL - モジュールが見つからない

- [ ] **ステップ 3: 最小限の実装を書く**

```typescript
// src/hooks/useActiveSection.ts
"use client";

import { useState, useEffect } from "react";

export function useActiveSection(sectionIds: string[]): string {
  const [activeSection, setActiveSection] = useState("");

  useEffect(() => {
    const observer = new IntersectionObserver(
      (entries) => {
        for (const entry of entries) {
          if (entry.isIntersecting) {
            setActiveSection(entry.target.id);
          }
        }
      },
      { rootMargin: "-50% 0px -50% 0px" }
    );

    for (const id of sectionIds) {
      const el = document.getElementById(id);
      if (el) observer.observe(el);
    }

    return () => observer.disconnect();
  }, [sectionIds]);

  return activeSection;
}
```

- [ ] **ステップ 4: テストがパスすることを確認**

実行: `npx vitest run src/hooks/useActiveSection.test.ts`
期待: PASS（4テスト）

- [ ] **ステップ 5: コミット**

```bash
git add src/hooks/useActiveSection.ts src/hooks/useActiveSection.test.ts
git commit -m "feat: add useActiveSection hook for nav highlight"
```

---

### タスク 3: useMagneticButton フック

**ファイル:**
- 新規: `src/hooks/useMagneticButton.ts`
- 新規: `src/hooks/useMagneticButton.test.ts`

- [ ] **ステップ 1: 失敗するテストを書く**

```typescript
// src/hooks/useMagneticButton.test.ts
import { describe, it, expect } from "vitest";
import { renderHook } from "@testing-library/react";
import { useMagneticButton } from "./useMagneticButton";

describe("useMagneticButton", () => {
  it("refとpositionステートを返す", () => {
    const { result } = renderHook(() => useMagneticButton());
    expect(result.current.ref).toBeDefined();
    expect(result.current.position).toEqual({ x: 0, y: 0 });
  });

  it("リセットハンドラーを返す", () => {
    const { result } = renderHook(() => useMagneticButton());
    expect(typeof result.current.handleMouseLeave).toBe("function");
  });

  it("マウス移動ハンドラーを返す", () => {
    const { result } = renderHook(() => useMagneticButton());
    expect(typeof result.current.handleMouseMove).toBe("function");
  });

  it("マウスリーブでpositionをリセットする", () => {
    const { result } = renderHook(() => useMagneticButton());
    result.current.handleMouseLeave();
    expect(result.current.position).toEqual({ x: 0, y: 0 });
  });
});
```

- [ ] **ステップ 2: テストが失敗することを確認**

実行: `npx vitest run src/hooks/useMagneticButton.test.ts`
期待: FAIL - モジュールが見つからない

- [ ] **ステップ 3: 最小限の実装を書く**

```typescript
// src/hooks/useMagneticButton.ts
"use client";

import { useRef, useState, useCallback } from "react";

interface Position {
  x: number;
  y: number;
}

export function useMagneticButton(strength = 0.3) {
  const ref = useRef<HTMLElement>(null);
  const [position, setPosition] = useState<Position>({ x: 0, y: 0 });

  const handleMouseMove = useCallback(
    (e: React.MouseEvent) => {
      const el = ref.current;
      if (!el) return;
      const rect = el.getBoundingClientRect();
      const centerX = rect.left + rect.width / 2;
      const centerY = rect.top + rect.height / 2;
      setPosition({
        x: (e.clientX - centerX) * strength,
        y: (e.clientY - centerY) * strength,
      });
    },
    [strength]
  );

  const handleMouseLeave = useCallback(() => {
    setPosition({ x: 0, y: 0 });
  }, []);

  return { ref, position, handleMouseMove, handleMouseLeave };
}
```

- [ ] **ステップ 4: テストがパスすることを確認**

実行: `npx vitest run src/hooks/useMagneticButton.test.ts`
期待: PASS（4テスト）

- [ ] **ステップ 5: コミット**

```bash
git add src/hooks/useMagneticButton.ts src/hooks/useMagneticButton.test.ts
git commit -m "feat: add useMagneticButton hook for magnetic hover effect"
```

---

### タスク 4: お問い合わせAPIルート

**ファイル:**
- 新規: `src/app/api/contact/route.ts`
- 新規: `src/app/api/contact/route.test.ts`

- [ ] **ステップ 1: 失敗するテストを書く**

テスト内容: 有効な入力で201を返す、名前なしで400、無効メールで400、カテゴリなしで400、メッセージなしで400、Resend失敗で500、電話番号なし（任意項目）で201。既存の `subscribe/route.test.ts` のパターンに従う（`vi.mock("resend")` でモック）。

- [ ] **ステップ 2: テストが失敗することを確認**

実行: `npx vitest run src/app/api/contact/route.test.ts`
期待: FAIL

- [ ] **ステップ 3: 最小限の実装を書く**

実装内容:
- `ContactRequestBody` インターフェース: name, email, phone?(任意), category, message
- `EMAIL_REGEX` と `VALID_CATEGORIES` でバリデーション
- `CATEGORY_LABELS` で種別のラベル変換
- Resend の `emails.send()` で hello@rstagency.com にメール送信
- 成功時201、バリデーションエラー時400、送信失敗時500を返却

- [ ] **ステップ 4: テストがパスすることを確認**

実行: `npx vitest run src/app/api/contact/route.test.ts`
期待: PASS（7テスト）

- [ ] **ステップ 5: コミット**

```bash
git add src/app/api/contact/route.ts src/app/api/contact/route.test.ts
git commit -m "feat: add contact form API route with Resend integration"
```

---

### タスク 5: HomeNavigation コンポーネント

**ファイル:**
- 新規: `src/components/home/HomeNavigation.tsx`
- 新規: `src/components/home/HomeNavigation.test.tsx`

**依存:** useLanguage（タスク1）, useActiveSection（タスク2）

- [ ] **ステップ 1: 失敗するテストを書く**

テスト内容: ロゴ画像の表示、6つのナビリンクと正しいhref、アクティブセクションのハイライト、JP/ENトグル切り替え、RESERVEボタン、ハンバーガーメニューの開閉、モバイルメニューのナビリンククリックで自動クローズ、固定配置。

- [ ] **ステップ 2: テストが失敗することを確認**

実行: `npx vitest run src/components/home/HomeNavigation.test.tsx`

- [ ] **ステップ 3: コンポーネントを実装**

実装の要点:
- 固定ヘッダー: 透明→blur+ダーク背景へのスクロール変化
- デスクトップ: `yoko-w.png`、モバイル: `mark-w.png`（`next/image`使用）
- 6つのナビリンク: アンダーライン拡張アニメーション、アクティブセクションをアクセントカラーでハイライト
- JP/ENトグル: `useLanguage` フック使用
- RESERVEボタン: アクセント背景、`href="#"` プレースホルダー
- モバイル: ハンバーガーでフルスクリーンオーバーレイ（`role="dialog"`）、`AnimatePresence` で表示
- モバイルナビリンク: クリックでメニュー自動クローズ
- `useActiveSection` で現在セクション検知

- [ ] **ステップ 4: テストがパスすることを確認**

- [ ] **ステップ 5: コミット**

```bash
git add src/components/home/HomeNavigation.tsx src/components/home/HomeNavigation.test.tsx
git commit -m "feat: add HomeNavigation with scroll transition, JP/EN toggle, mobile menu"
```

---

### タスク 6: HomeHero コンポーネント

**ファイル:**
- 新規: `src/components/home/HomeHero.tsx`
- 新規: `src/components/home/HomeHero.test.tsx`

**依存:** useMagneticButton（タスク3）, useLanguage（タスク1）

- [ ] **ステップ 1: 失敗するテストを書く**

テスト内容: アクセントカラーでハイライトされた見出し、英語タグライン、CTAボタン、スクロールインジケーター、写真プレースホルダー、フルビューポート高さ。

- [ ] **ステップ 2: テストが失敗することを確認**

- [ ] **ステップ 3: コンポーネントを実装**

実装の要点:
- `min-h-screen`、ダーク背景
- 非対称分割: 左60%見出し、右40%写真プレースホルダー
- 見出し各行: `motion.span` でスタガードリビール（y-transform）
- 見出し中の特定テキストをアクセントカラーに
- 英語タグライン: 字間広めの大文字
- CTA: `useMagneticButton` でホバー効果
- 写真: `clipPath: polygon()` で斜めカットマスク
- スクロールインジケーター: `[writing-mode:vertical-rl]` + `animate-bounce`

- [ ] **ステップ 4: テストがパスすることを確認**

- [ ] **ステップ 5: コミット**

```bash
git add src/components/home/HomeHero.tsx src/components/home/HomeHero.test.tsx
git commit -m "feat: add HomeHero with asymmetric layout, text reveal, magnetic CTA"
```

---

### タスク 7: HomeConcept コンポーネント

**ファイル:**
- 新規: `src/components/home/HomeConcept.tsx`
- 新規: `src/components/home/HomeConcept.test.tsx`

- [ ] **ステップ 1: 失敗するテストを書く**

テスト内容: セクションID "concept"、CONCEPTラベル、段落テキスト、写真プレースホルダー、オフホワイト背景。

- [ ] **ステップ 2: テストが失敗することを確認**

- [ ] **ステップ 3: コンポーネントを実装**

実装の要点:
- オフホワイト背景 (#F5F2EE)、ダークテキスト
- デスクトップ: 縦書き「CONCEPT」ラベル（`writing-mode: vertical-rl`）、モバイル: 水平小テキスト
- 段落各行: `whileInView` でスタガードフェードイン
- 写真プレースホルダー: ダークグラデーション背景

- [ ] **ステップ 4: テストがパスすることを確認**

- [ ] **ステップ 5: コミット**

```bash
git add src/components/home/HomeConcept.tsx src/components/home/HomeConcept.test.tsx
git commit -m "feat: add HomeConcept with vertical label, staggered text reveal"
```

---

### タスク 8: HomeKeyNumbers コンポーネント

**ファイル:**
- 新規: `src/components/home/HomeKeyNumbers.tsx`
- 新規: `src/components/home/HomeKeyNumbers.test.tsx`

- [ ] **ステップ 1: 失敗するテストを書く**

テスト内容: セクションID、4つの数値、英語ラベルと日本語ラベル、ダーク背景。

- [ ] **ステップ 2: テストが失敗することを確認**

- [ ] **ステップ 3: コンポーネントを実装**

実装の要点:
- ダーク背景、特大セリフ体数字（clampで流体サイジング）
- デスクトップ: 4カラムグリッド、細いグレー縦線ボーダー。モバイル: 2x2
- 小さなサンセリフ体ラベル（英語+日本語）
- `whileInView` フェードインアニメーション

- [ ] **ステップ 4: テストがパスすることを確認**

- [ ] **ステップ 5: コミット**

```bash
git add src/components/home/HomeKeyNumbers.tsx src/components/home/HomeKeyNumbers.test.tsx
git commit -m "feat: add HomeKeyNumbers with oversized serif numbers"
```

---

### タスク 9: HomeFacility コンポーネント

**ファイル:**
- 新規: `src/components/home/HomeFacility.tsx`
- 新規: `src/components/home/HomeFacility.test.tsx`

- [ ] **ステップ 1: 失敗するテストを書く**

テスト内容: セクションID "facility"、コートスペック、全アメニティ、FACILITYラベル、ダーク背景。

- [ ] **ステップ 2: テストが失敗することを確認**

- [ ] **ステップ 3: コンポーネントを実装**

実装の要点:
- ダーク背景、FACILITYラベル
- コートショーケース: 16:9画像プレースホルダー、グラデーションオーバーレイ、下部にスペック重ね表示
- アメニティ: 細い区切り線のテキストリスト、ホバーで説明表示
- モバイル: スタックレイアウト

- [ ] **ステップ 4: テストがパスすることを確認**

- [ ] **ステップ 5: コミット**

```bash
git add src/components/home/HomeFacility.tsx src/components/home/HomeFacility.test.tsx
git commit -m "feat: add HomeFacility with court showcase and expandable amenities"
```

---

### タスク 10: HomeServices コンポーネント

**ファイル:**
- 新規: `src/components/home/HomeServices.tsx`
- 新規: `src/components/home/HomeServices.test.tsx`

- [ ] **ステップ 1: 失敗するテストを書く**

テスト内容: セクションID "services"、5つのサービス番号、日本語・英語タイトル、説明文、`data-service-row`属性による背景色交互確認。

- [ ] **ステップ 2: テストが失敗することを確認**

- [ ] **ステップ 3: コンポーネントを実装**

実装の要点:
- 5つのフルワイド行、ダーク/オフホワイト背景交互
- 各行: 画像60% + テキスト40%、左右交互
- サービス番号: 大きなアクセントセリフ体、日本語タイトル、英語サブタイトル、説明文
- `data-service-row` 属性でテスト対象指定
- スクロールリビールアニメーション
- モバイル: すべてスタック（画像上、テキスト下）

- [ ] **ステップ 4: テストがパスすることを確認**

- [ ] **ステップ 5: コミット**

```bash
git add src/components/home/HomeServices.tsx src/components/home/HomeServices.test.tsx
git commit -m "feat: add HomeServices with 5 alternating full-width rows"
```

---

### タスク 11: HomePricing コンポーネント

**ファイル:**
- 新規: `src/components/home/HomePricing.tsx`
- 新規: `src/components/home/HomePricing.test.tsx`

- [ ] **ステップ 1: 失敗するテストを書く**

テスト内容: セクションID "pricing"、近日公開見出し、Instagram言及、3つの料金プラン、COMING SOON×3、レンタル案内、ダーク背景。

- [ ] **ステップ 2: テストが失敗することを確認**

- [ ] **ステップ 3: コンポーネントを実装**

実装の要点:
- ダーク背景
- 近日公開: 大きなセリフ体見出し、Instagramリンク（アクセントカラー）
- 3カラムグリッド: 細いボーダー(#333)、角丸なし、シャドウなし
- おすすめプランにアクセント上部ボーダー
- レンタル案内: アクセントドットマーカー
- モバイル: 縦積み

- [ ] **ステップ 4: テストがパスすることを確認**

- [ ] **ステップ 5: コミット**

```bash
git add src/components/home/HomePricing.tsx src/components/home/HomePricing.test.tsx
git commit -m "feat: add HomePricing with coming soon notice and 3-tier preview"
```

---

### タスク 12: HomeFounder コンポーネント

**ファイル:**
- 新規: `src/components/home/HomeFounder.tsx`
- 新規: `src/components/home/HomeFounder.test.tsx`

- [ ] **ステップ 1: 失敗するテストを書く**

テスト内容: セクションID "founder"、FOUNDERラベル、日本語・英語名、タイムラインエントリー、PRESSラベル、PR TIMESリンク（正しいhrefとtarget）。

- [ ] **ステップ 2: テストが失敗することを確認**

- [ ] **ステップ 3: コンポーネントを実装**

実装の要点:
- ダークグラデーション背景（将来のシネマティック写真のプレースホルダー）
- 半透明オーバーレイでテキスト可読性確保
- FOUNDERラベル、大きな名前、英語サブタイトル
- タイムラインエントリー: スタガードフェードイン、年号をアクセント/太字で強調
- プレスセクション: 記事タイトル + PR TIMESリンク（新しいタブ、アクセントカラー、ホバーでアンダーライン）

- [ ] **ステップ 4: テストがパスすることを確認**

- [ ] **ステップ 5: コミット**

```bash
git add src/components/home/HomeFounder.tsx src/components/home/HomeFounder.test.tsx
git commit -m "feat: add HomeFounder with timeline bio and PR TIMES press link"
```

---

### タスク 13: HomeAccess コンポーネント

**ファイル:**
- 新規: `src/components/home/HomeAccess.tsx`
- 新規: `src/components/home/HomeAccess.test.tsx`

- [ ] **ステップ 1: 失敗するテストを書く**

テスト内容: セクションID "access"、タイトル付きiframe、住所、営業時間、メール、3つの駅アクセスルート、アクセントカラーの所要時間、駐車場案内、オフホワイト背景。

- [ ] **ステップ 2: テストが失敗することを確認**

- [ ] **ステップ 3: コンポーネントを実装**

実装の要点:
- オフホワイト背景
- 全幅Google Maps iframe（デスクトップ450px、モバイル300px）
- マップ下部2カラム: 左に住所、右にアクセスルート
- ルート: 駅名太字、所要時間アクセントカラー、細い下部ボーダー
- 駐車場案内: ミュートグレー

- [ ] **ステップ 4: テストがパスすることを確認**

- [ ] **ステップ 5: コミット**

```bash
git add src/components/home/HomeAccess.tsx src/components/home/HomeAccess.test.tsx
git commit -m "feat: add HomeAccess with Google Maps iframe and station directions"
```

---

### タスク 14: HomeContact コンポーネント

**ファイル:**
- 新規: `src/components/home/HomeContact.tsx`
- 新規: `src/components/home/HomeContact.test.tsx`

**依存:** お問い合わせAPIルート（タスク4）

- [ ] **ステップ 1: 失敗するテストを書く**

テスト内容: セクションID "contact"、ラベル付きフォームフィールド、送信ボタン、連絡先情報（GET IN TOUCH、メール、Instagram）、fetchでのフォーム送信、成功/エラーメッセージ、ダーク背景。

- [ ] **ステップ 2: テストが失敗することを確認**

- [ ] **ステップ 3: コンポーネントを実装**

実装の要点:
- ダーク背景、2カラム（左フォーム、右情報。モバイルでは逆順）
- フォーム: 下線のみの入力欄、アクセントフォーカスボーダー、selectドロップダウン、textarea
- `/api/contact` にfetchで送信、成功/エラー状態を表示
- 連絡先情報: GET IN TOUCHラベル、アクセントカラーのメール、Instagram、住所、営業時間
- モバイル: 情報を上、フォームを下、フルワイド入力欄とボタン

- [ ] **ステップ 4: テストがパスすることを確認**

- [ ] **ステップ 5: コミット**

```bash
git add src/components/home/HomeContact.tsx src/components/home/HomeContact.test.tsx
git commit -m "feat: add HomeContact with underline-style form and Resend integration"
```

---

### タスク 15: HomeFooter コンポーネント

**ファイル:**
- 新規: `src/components/home/HomeFooter.tsx`
- 新規: `src/components/home/HomeFooter.test.tsx`

- [ ] **ステップ 1: 失敗するテストを書く**

テスト内容: footer要素、ロゴ画像、6つのナビリンク、メールリンク、コピーライト、住所、アクセント上部セパレーター。

- [ ] **ステップ 2: テストが失敗することを確認**

- [ ] **ステップ 3: コンポーネントを実装**

実装の要点:
- ダーク背景、アクセントカラー1pxセパレーター
- 3カラム: ロゴ（`yoko-w.png`）、ナビリンク、メール
- 最下部行: 左にコピーライト、右に住所
- モバイル: 1カラムスタック
- `"use client"` 不要（インタラクティビティなし）

- [ ] **ステップ 4: テストがパスすることを確認**

- [ ] **ステップ 5: コミット**

```bash
git add src/components/home/HomeFooter.tsx src/components/home/HomeFooter.test.tsx
git commit -m "feat: add HomeFooter with accent separator, 3-column layout"
```

---

### タスク 16: ページ構成

**ファイル:**
- 変更: `src/app/page.tsx`

- [ ] **ステップ 1: page.tsx を新しい構成に更新**

```tsx
// src/app/page.tsx
import { LanguageProvider } from "@/hooks/useLanguage";
import HomeNavigation from "@/components/home/HomeNavigation";
import HomeHero from "@/components/home/HomeHero";
import HomeConcept from "@/components/home/HomeConcept";
import HomeKeyNumbers from "@/components/home/HomeKeyNumbers";
import HomeFacility from "@/components/home/HomeFacility";
import HomeServices from "@/components/home/HomeServices";
import HomePricing from "@/components/home/HomePricing";
import HomeFounder from "@/components/home/HomeFounder";
import HomeAccess from "@/components/home/HomeAccess";
import HomeContact from "@/components/home/HomeContact";
import HomeFooter from "@/components/home/HomeFooter";

export default function Home() {
  return (
    <LanguageProvider>
      <main>
        <HomeNavigation />
        <HomeHero />
        <HomeConcept />
        <HomeKeyNumbers />
        <HomeFacility />
        <HomeServices />
        <HomePricing />
        <HomeFounder />
        <HomeAccess />
        <HomeContact />
        <HomeFooter />
      </main>
    </LanguageProvider>
  );
}
```

- [ ] **ステップ 2: 全テストを実行**

実行: `npx vitest run`
期待: 全てPASS

- [ ] **ステップ 3: ビルドしてエラーがないことを確認**

実行: `npm run build`
期待: ビルド成功

- [ ] **ステップ 4: コミット**

```bash
git add src/app/page.tsx
git commit -m "feat: compose Home LP with all new sections and LanguageProvider"
```

---

### タスク 17: 統合テストとカバレッジ修正

**ファイル:**
- 変更: カバレッジギャップのあるファイル

- [ ] **ステップ 1: カバレッジ付きで全テストスイートを実行**

実行: `npx vitest run --coverage`
出力を確認してカバーされていない分岐や行を特定

- [ ] **ステップ 2: 100%カバレッジのために不足テストを追加**

カバレッジレポートを確認し、カバーされていないパス（フックのエッジケース、コンポーネントの未テスト分岐など）のテストを追加。

- [ ] **ステップ 3: カバレッジを再実行して100%を確認**

実行: `npx vitest run --coverage`
期待: statements, branches, functions, lines すべて100%

- [ ] **ステップ 4: リントを実行**

実行: `npm run lint`
期待: エラーなし

- [ ] **ステップ 5: 最終コミット**

```bash
git add -A
git commit -m "test: achieve 100% coverage for home LP components"
```
